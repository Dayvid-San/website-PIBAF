import Styles from './styles.module.scss';


const MainSection = () => {
    const text = '';

    return (
        <div className={Styles.container}>
            <p>
                {text}
            </p>
        </div>
    );
};

export default MainSection;