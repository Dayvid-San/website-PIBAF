import Styles from 'styles.module.scss';


const Main = () => {
    const text = '';

    return (
        <div className={Styles.container}>
            <p>
                {text}
            </p>
        </div>
    );
};

export default Main;